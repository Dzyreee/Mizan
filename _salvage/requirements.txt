# Mizan backend — Phase 0 dependencies
openai>=1.40.0          # OpenAI-compatible SDK, pointed at Fanar base_url
httpx>=0.27.0           # for endpoints that are not OpenAI-shaped (TTS/STT/translate)
python-dotenv>=1.0.1    # load FANAR_API_KEY from .env
pytest>=8.0.0           # faraid calculator unit tests (Phase 1)

# Added in later phases (kept here for reference, safe to install now):
fastapi>=0.110.0        # backend API (Phase 2+)
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9 # audio file uploads for Aura STT
